#!/bin/bash

choice=$(echo "Lock
Sleep
Hibernate
Shutdown
Reboot" | rofi -dmenu -lines 5 -width -20 -p ">>")

case "$choice" in
	Hibernate)
		systemctl hibernate
		;;

	Sleep)
		systemctl suspend
		;;

	Shutdown)
		poweroff
		;;

	Reboot)
		reboot
		;;

	Lock)
		echo "Locking not yet implemented"
		;;

	*)
		echo "$choice not an option"
esac
